TODO


TimerEvent ほか、SS上で動くもの
	アイデアベースで、要る機能から作る。Filters
	TimeEvent
		setInterval(interval)	-return timerId
		timerList							-return timerIds
		remove(timerId)

	BufferPool
		filled
		updated
		bufferList

	KeyValueStore
		set(key,value)
		get(key)
		keys
		remove(key)
		clear

	TranslateFilter
		???

	MessagePack
		ちょうどよくハッカソンあったので積んでみた。
		bufferとか、重量の有る物を外部に出すときに使用したい。
		今のところmsgpackブランチのみOn。
		

TranslateFilterの追加
	任意のストリーム　＞　アクション変換を可能にする機構を、作成してさらに外部化したい。
	具体的には、外部コンパイラの吐き出したメッセージストリームを、
		特定のST2APIに翻訳する、中継機(TranslateFilter)を足し引き可能にしたい。できるだけ動的に。
		うーーーーんどうすれば。
		音楽のシーケンサみたいな感じに追加したいが、ClassにしてArrayの連続の中に差し込むとかそういうのかな、、めんどい。


Sequence式の書式
	どこかに記録を作らないと参照できない。ある程度固めたら書く。
	
	02/02/13 16:45:17
	sublimesocket@コマンド:{JSONPARAM}>コマンド2:{JSONPARAM}>コマンド3:{JSONPARAM}>,,
	
	とかが可能。シーケンシャルな処理を実行するようにセットできる
	TimerEventとかは、そこから先のSequenceをすべて吸収して繰り返す。


OutputAPIの整理
	外部からのrequestも可能なので、InputAPIの拡張と言える。
	pushで押し付ける設定を外部から注入されれば、定期的に叩き付けることが可能。
	shakehandが済んだらOK、という。
	定期的に叩く外部、っていうのも、フィルタ的な感じでTranslateが必要か。


やりたいけどどうやったらいいか判らないもの
	STのConsoleの内容をそのまま送る
		printとかを使っての出力は可能だけど、めんどいし、、

	サジェストからのアクション
		やりたい


以下メモ

概念区分け
	outside/inside OOOOO
	全機能について、内部(SS上)と、外部(WebSocketの向こう側)の2つがありえそう。
	