TODO


TimerEvent ほか、SS上で動くもの
	アイデアベースで、要る機能から作る。Filters
	TimeEvent
		setInterval(interval)	-return timerId
		timerList							-return timerIds
		remove(timerId)

	BufferPool
		filled
		updated
		bufferList

	KeyValueStoreとは名ばかりのグローバルな値貯蔵庫
		√set(key,value)
		√get(key)
		keys
		remove(key)
		clear

√	defineFilter
		√フィルタの定義が可能

√	runFilter
		√フィルタの実行が可能

√	setEvent
		√イベントに対してリスナをセット可能

√	outputMessage
		√リスナに対して簡単なメッセージをセット可能

√	eval エラーハンドリング
		√エラー出たとき封印せねば。

		√eval 変数の定義を自力でできるのか？→無理。名前が異なるヤツを数種類用意しておくんだなー。

√	MessagePack
		√ちょうどよくハッカソンあったので積んでみた。
		bufferとか、重量の有る物を外部に出すときに使用したい。
		今のところmsgpackブランチのみOn。
		

√	TranslateFilterの概念追加
	√defineFilter -> runFilter


√	runSetting/TestRun
	必要。ファイル位置を読み込ませて、書いてあるのを順に実行する。改行とtabは無視される。
	runSetting


viewファイルの識別を行わないとな。
	targetSettingみたいなのを行ってからrepl、で良い気がする。
	それで、セット先のviewの有無が決まる。
	開いてないファイルとかもあり得る。
	evalでの対象を分けてやれば良いか。

SublimeSocketAPI -Sequence
	sublimesocket @ API : {JSONPARAM} -> API2 : {JSONPARAM} -> API3 : {JSONPARAM}>,,
	

OutputAPIの整理
	外部からのrequestも可能なので、InputAPIの拡張と言える。
	pushで押し付ける設定を外部から注入されれば、定期的に叩き付けることが可能。
	shakehandが済んだらOK、という。
	定期的に叩く外部、っていうのも、フィルタ的な感じでTranslateが必要か。


テスト
	書いた方が良い。
	outputMessageを当てにして、外部の物を一連で実行するようにしよう。


やりたいけどどうやったらいいか判らないもの
	STのConsoleの内容をそのまま送る
		printとかを使っての出力は可能だけど、めんどいし、、

	サジェストからのアクション
		やりたい

